dotnet
  --info
  --list-sdks
  --list-runtimes
  --version

  add
    package <package>
    reference <targetProject> <referenceProject>
    sln add <path/ProjectName.csproj>

  build <project>

  clean

  help <topic>

  list

  new
    --help / -h

    -f <framework>
      latestmajor
        Uses the highest major number, for example, 7.0 in August 2019, 8.0 in October 2019, 9.0 in November 2020, 10.0 in November 2021.

      latest
        Uses the highest major and highest minor number, for example, 7.2 in 2017, 7.3 in 2018, 8 in 2019, perhaps 10.1 in early 2022.

      preview
        Uses the highest available preview version, for example, 10.0 in July 2021 with .NET 6.0 Preview 6 installed.

    --list / -l
    -n <projectName>
    -o <outDir>

    classlib
    console
    globaljson --sdk-version <sdkVersion>
    xunit

  pack -c <Debug|Release>

  publish
    -c <Debug|Release>
    -r <win-x64|osx-x64|osx-arm64|linux-x64|linux-arm64|others>
    /p:PublishSingleFile=<false|true>
    -p:PublishTrimmed=<False|True>
    -p:TrimMode=<Link|copyused|Others>

  remove

  restore

  run --configuration <Debug|Release>

  sdk check

  test

  Update-Package Newtonsoft.Json -ToHighestMinor
  Update-Package Newtonsoft.Json -ToHighestPatch

  

dotnet-core-uninstall remove --all-previews-but-latest --sdk

dotnet tool install -g upgrade-assistant

Links
  https://aka.ms/new-console-template
  https://dotnetfiddle.net/
  https://devblogs.microsoft.com/dotnet/
  http://computerstufftheydidntteachyou.com/

